import Job from './models/Job';
import { fetchData, postData } from './utils/fetchUtil';
import { parseJSON, parseCSV } from './utils/parseUtil';

class gravityAI {
  constructor(apiKey, productId) {
    this.apiKey = apiKey;
    this.productId = productId;
  }
  
  // Other SDK methods using utilities and models
}

export default SDK;